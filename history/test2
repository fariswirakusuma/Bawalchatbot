user:whats the difference between #ifndef, #ifdef and #pragma in cpp
assistant:
Here's a brief explanation of what each of these three directives in C++ do:

1. `#ifndef`:
The `#ifndef` directive is used to conditionally include a header file. It checks whether the `#define` macro in the included header file has been defined before including the header again. If the `#define` macro has not been defined, the `#ifndef` directive will not include the header file, and the following code will not be executed.
```cpp
#include "headers.h"

// The following code will not be executed if headers.h has not been included
int main() {
    #ifndef headers.h
        printf("Headers.h not included\n");
    #else
        printf("Headers.h included\n");
    #endif
    return 0;
}
```

2. `#ifdef`:
The `#ifdef` directive is similar to `#ifndef`, but it checks whether a specific macro has been defined before including a specific header file. If the `#define` macro has been defined, the `#ifdef` directive will include the header file, and the code following the `#ifdef` directive will be executed. If the `#define` macro has not been defined, the `#ifdef`
